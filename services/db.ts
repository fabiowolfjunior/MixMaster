
// services/db.ts
// Deprecated in favor of services/api.ts
// Keeping empty or minimal methods to prevent build errors if imports linger
export const getSales = () => [];
export const getIngredients = () => [];
export const getSuppliers = () => [];
export const getProducts = () => [];
export const seedDatabase = () => { };
// ... add others if needed or just let them break to find usages. 
// Ideally we want to find usages.
